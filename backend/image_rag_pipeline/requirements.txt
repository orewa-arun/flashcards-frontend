# Core dependencies for Image-RAG Pipeline
# Optimized for Railway deployment
# NOTE: PyTorch CPU-only is installed via railway-build.sh to reduce memory usage
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Vector database
qdrant-client==1.7.3

# PDF processing
PyMuPDF==1.26.4

# Image processing
Pillow==11.3.0

# PyTorch - Install CPU-only version via build script (railway-build.sh)
# This reduces install size from ~2GB+ to ~500MB
# torch and torchvision are installed in railway-build.sh

# CLIP model (requires torch to be installed first)
open-clip-torch==2.24.0

# Utilities
python-dotenv==1.0.0

# LangChain for conversational RAG
langchain==0.1.0
langchain-core==0.1.10
langchain-google-genai==0.0.6

# Additional dependencies
google-generativeai==0.3.2
pydantic==2.10.4

